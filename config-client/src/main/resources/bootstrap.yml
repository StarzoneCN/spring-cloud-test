spring:
  application:
    name: config-client
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: default
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/
management:
  security:
    enabled: false




